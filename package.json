{
  "name": "maplekit",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:github": "cross-env DEPLOY_TARGET=github vite build",
    "build:vercel": "cross-env DEPLOY_TARGET=vercel vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "cross-env": "^10.0.0",
    "vite": "^7.1.2"
  }
}
